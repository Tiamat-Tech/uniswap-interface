diff --git a/node_modules/react-native-context-menu-view/.bun-tag-32b89f3c12c24fb7 b/.bun-tag-32b89f3c12c24fb7
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/react-native-context-menu-view/.bun-tag-720d6033c0d8c08e b/.bun-tag-720d6033c0d8c08e
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/react-native-context-menu-view/.bun-tag-77a3a5fab7180601 b/.bun-tag-77a3a5fab7180601
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/android/src/main/java/com/mpiannucci/reactnativecontextmenu/ContextMenuView.java b/android/src/main/java/com/mpiannucci/reactnativecontextmenu/ContextMenuView.java
index 9fc8eeb9b6b66061fca818a17127ff6f21df3126..a8d1b6e8d23453c6411d1a8d4081a4a2fd3fcfe7 100644
--- a/android/src/main/java/com/mpiannucci/reactnativecontextmenu/ContextMenuView.java
+++ b/android/src/main/java/com/mpiannucci/reactnativecontextmenu/ContextMenuView.java
@@ -1,11 +1,14 @@
 package com.mpiannucci.reactnativecontextmenu;
 
+import android.app.Activity;
 import android.content.Context;
+import android.content.ContextWrapper;
 import android.content.res.ColorStateList;
 import android.content.res.Resources;
 import android.graphics.Color;
 import android.graphics.drawable.Drawable;
 import android.os.Build;
+import android.os.SystemClock;
 import android.text.SpannableString;
 import android.text.Spanned;
 import android.text.style.ForegroundColorSpan;
@@ -25,6 +28,7 @@ import com.facebook.react.bridge.ReadableArray;
 import com.facebook.react.bridge.ReadableMap;
 import com.facebook.react.bridge.WritableArray;
 import com.facebook.react.bridge.WritableMap;
+import com.facebook.react.uimanager.events.NativeGestureUtil;
 import com.facebook.react.uimanager.events.RCTEventEmitter;
 import com.facebook.react.views.view.ReactViewGroup;
 
@@ -37,6 +41,10 @@ public class ContextMenuView extends ReactViewGroup implements View.OnCreateCont
 
     boolean cancelled = true;
 
+    boolean awaitingMenuCloseFlush = false;
+
+    int[] longPressStartLocation = new int[2];
+
     protected boolean dropdownMenuMode = false;
 
     protected boolean disabled = false;
@@ -61,12 +69,41 @@ public class ContextMenuView extends ReactViewGroup implements View.OnCreateCont
 
             @Override
             public void onLongPress(MotionEvent e) {
+                int[] location = new int[2];
+                getLocationOnScreen(location);
+
+                int dx = location[0] - longPressStartLocation[0];
+                int dy = location[1] - longPressStartLocation[1];
+                double distance = Math.sqrt(dx * dx + dy * dy);
+                // Cancel long press if the user moves their finger more than 10 pixels
+                // (e.g. the context menu is used inside the scrollable component and it
+                // moves as the user scrolls)
+                if (distance > 10) {
+                    cancelled = true;
+                    return;
+                }
+
                 if (!dropdownMenuMode && !disabled) {
                     if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {
+                        cancelChildTouchHandling(e);
                         showContextMenu(e.getX(), e.getY());
                     }
                 }
             }
+
+            @Override
+            public boolean onDoubleTap(MotionEvent e) {
+                if (dropdownMenuMode && !disabled) {
+                    showContextMenu(e.getX(), e.getY());
+                }
+                return super.onSingleTapConfirmed(e);
+            }
+
+            @Override
+            public boolean onDown(MotionEvent e) {
+                getLocationOnScreen(longPressStartLocation);
+                return super.onDown(e);
+            }
         });
     }
 
@@ -88,6 +125,77 @@ public class ContextMenuView extends ReactViewGroup implements View.OnCreateCont
         return true;
     }
 
+    @Override
+    protected void onDetachedFromWindow() {
+        // A view detached while its menu is open never gets the onWindowFocusChanged close
+        // signal, so clear the flag here — a recycled instance (Fabric view pooling) would
+        // otherwise fire a spurious cancel on its next focus regain. No inline flush: RNGH
+        // already cancels a detaching view's in-flight gestures, and notifyNativeGesture* on a
+        // view mid-detach may have no reachable root.
+        awaitingMenuCloseFlush = false;
+        dismissOpenContextMenu();
+        super.onDetachedFromWindow();
+    }
+
+    @Override
+    public boolean showContextMenu(float x, float y) {
+        boolean shown = super.showContextMenu(x, y);
+        // The system menu is a separate focusable window; our window regaining focus is the
+        // close signal (there is no public close callback). See onWindowFocusChanged.
+        awaitingMenuCloseFlush = shown;
+        return shown;
+    }
+
+    // While the system context menu window is open or animating closed, it consumes touch
+    // streams whose tail (ACTION_UP/CANCEL) never reaches the app window. A tap landing in that
+    // window leaves RN/RNGH with a started-but-never-finalized gesture, and every
+    // RNGH-Pressable-backed control stays cancelled until the app is killed. Once our window
+    // regains focus, force-finalize whatever the menu swallowed with the same
+    // started/disallow-intercept/ended sequence used when the menu opens.
+    @Override
+    public void onWindowFocusChanged(boolean hasWindowFocus) {
+        super.onWindowFocusChanged(hasWindowFocus);
+        if (hasWindowFocus && awaitingMenuCloseFlush) {
+            awaitingMenuCloseFlush = false;
+            long now = SystemClock.uptimeMillis();
+            MotionEvent cancel = MotionEvent.obtain(now, now, MotionEvent.ACTION_CANCEL, 0f, 0f, 0);
+            try {
+                NativeGestureUtil.notifyNativeGestureStarted(this, cancel);
+                requestDisallowInterceptTouchEvent(true);
+                NativeGestureUtil.notifyNativeGestureEnded(this, cancel);
+            } finally {
+                cancel.recycle();
+            }
+        }
+    }
+
+    private void dismissOpenContextMenu() {
+        Context context = getContext();
+        while (context instanceof ContextWrapper) {
+            if (context instanceof Activity) {
+                ((Activity) context).closeContextMenu();
+                return;
+            }
+            context = ((ContextWrapper) context).getBaseContext();
+        }
+    }
+
+    // Prevent child onPress from firing after a long-press opens the native context menu.
+    // Cancel RN with the real touch event before RNGH's synthetic cancellation, then cancel RNGH
+    // recognizers before Android's context menu consumes the rest of the touch stream.
+    // Balance NativeGestureUtil immediately because the native menu may consume ACTION_UP/CANCEL.
+    private void cancelChildTouchHandling(MotionEvent e) {
+        for (int i = 0; i < getChildCount(); i++) {
+            View child = getChildAt(i);
+            child.setPressed(false);
+            child.cancelLongPress();
+        }
+
+        NativeGestureUtil.notifyNativeGestureStarted(this, e);
+        requestDisallowInterceptTouchEvent(true);
+        NativeGestureUtil.notifyNativeGestureEnded(this, e);
+    }
+
     @Override
     public void onCreateContextMenu(ContextMenu contextMenu, View view, ContextMenu.ContextMenuInfo contextMenuInfo) {
         contextMenu.clear();
diff --git a/ios/ContextMenuView.m b/ios/ContextMenuView.m
index 7f8ee75bae377ea9c1b02ee6b7e3e834e16c6520..49893beffa2f28c75295647b4836adeb35a253dc 100644
--- a/ios/ContextMenuView.m
+++ b/ios/ContextMenuView.m
@@ -18,6 +18,7 @@ - (UIMenuElement*) createMenuElementForAction:(ContextMenuAction *)action atInde
 @implementation ContextMenuView {
   BOOL _cancelled;
   UIView *_customView;
+  BOOL _observingBackground;
 }
 
 - (id) init {
@@ -32,6 +33,49 @@ - (id) init {
     return self;
 }
 
+- (void)dealloc {
+    [self stopObservingBackground];
+}
+
+// Close the context menu when the app is backgrounded.
+- (void)startObservingBackground {
+    if (_observingBackground) {
+        return;
+    }
+    _observingBackground = YES;
+    [[NSNotificationCenter defaultCenter] addObserver:self
+                                             selector:@selector(dismissOpenContextMenu)
+                                                 name:UIApplicationDidEnterBackgroundNotification
+                                               object:nil];
+}
+
+- (void)stopObservingBackground {
+    if (!_observingBackground) {
+        return;
+    }
+    _observingBackground = NO;
+    [[NSNotificationCenter defaultCenter] removeObserver:self
+                                                    name:UIApplicationDidEnterBackgroundNotification
+                                                  object:nil];
+}
+
+- (void)dismissOpenContextMenu {
+    if (@available(iOS 14.0, *)) {
+        [self dismissContextMenuInteractionsOnView:self];
+        for (UIView *subview in self.reactSubviews) {
+            [self dismissContextMenuInteractionsOnView:subview];
+        }
+    }
+}
+
+- (void)dismissContextMenuInteractionsOnView:(UIView *)view API_AVAILABLE(ios(14.0)) {
+    for (id<UIInteraction> interaction in view.interactions) {
+        if ([interaction isKindOfClass:[UIContextMenuInteraction class]]) {
+            [(UIContextMenuInteraction *)interaction dismissMenu];
+        }
+    }
+}
+
 - (void)insertReactSubview:(UIView *)subview atIndex:(NSInteger)atIndex
 {
   if ([subview.nativeID isEqualToString:@"ContextMenuPreview"]) {
@@ -91,6 +135,7 @@ - (nullable UIContextMenuConfiguration *)contextMenuInteraction:(nonnull UIConte
 
 - (void)contextMenuInteraction:(UIContextMenuInteraction *)interaction willDisplayMenuForConfiguration:(UIContextMenuConfiguration *)configuration animator:(id<UIContextMenuInteractionAnimating>)animator API_AVAILABLE(ios(13.0)) {
   _cancelled = true;
+  [self startObservingBackground];
 }
 
 - (void)contextMenuInteraction:(UIContextMenuInteraction *)interaction willPerformPreviewActionForMenuWithConfiguration:(nonnull UIContextMenuConfiguration *)configuration animator:(nonnull id<UIContextMenuInteractionCommitAnimating>)animator API_AVAILABLE(ios(13.0)) {
@@ -100,6 +145,7 @@ - (void)contextMenuInteraction:(UIContextMenuInteraction *)interaction willPerfo
 }
 
 - (void)contextMenuInteraction:(UIContextMenuInteraction *)interaction willEndForConfiguration:(UIContextMenuConfiguration *)configuration animator:(id<UIContextMenuInteractionAnimating>)animator API_AVAILABLE(ios(13.0)) {
+  [self stopObservingBackground];
   if (_cancelled && self.onCancel) {
     self.onCancel(@{});
   }
