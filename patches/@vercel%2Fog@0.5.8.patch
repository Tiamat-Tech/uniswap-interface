diff --git a/node_modules/@vercel/og/.bun-tag-a114a3b3cda5ad27 b/.bun-tag-a114a3b3cda5ad27
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@vercel/og/.bun-tag-db9c1657a488608 b/.bun-tag-db9c1657a488608
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/node_modules/@vercel/og/.bun-tag-e59db02cac51bf25 b/.bun-tag-e59db02cac51bf25
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/index.edge.js b/dist/index.edge.js
index 5187f88ab3375622199e7628cf15c00ffef65584..8095df63793b6e5a4ef7c4e1990812bcb8fa1e19 100644
--- a/dist/index.edge.js
+++ b/dist/index.edge.js
@@ -18673,8 +18673,8 @@ var Resvg2 = class extends Resvg {
 };
 
 // src/index.edge.ts
-import resvg_wasm from "./resvg.wasm?module";
-import yoga_wasm from "./yoga.wasm?module";
+import resvg_wasm from "./resvg.wasm";
+import yoga_wasm from "./yoga.wasm";
 
 // src/emoji/index.ts
 var U200D = String.fromCharCode(8205);
@@ -18803,24 +18803,30 @@ async function render(satori, resvg, opts, defaultFonts, element) {
       value: options.width
     }
   });
-  return resvgJS.render().asPng();
+  try {
+    const renderedImage = resvgJS.render();
+    try {
+      return renderedImage.asPng();
+    } finally {
+      renderedImage.free();
+    }
+  } finally {
+    resvgJS.free();
+  }
 }
 
 // src/index.edge.ts
 var initializedResvg = initWasm(resvg_wasm);
 var initializedYoga = initYoga(yoga_wasm).then((yoga2) => Ll(yoga2));
-var fallbackFont = fetch(new URL("./noto-sans-v27-latin-regular.ttf", import.meta.url)).then((res) => res.arrayBuffer());
 var ImageResponse = class {
   constructor(element, options = {}) {
     const result = new ReadableStream({
       async start(controller) {
         await initializedYoga;
         await initializedResvg;
-        const fontData = await fallbackFont;
         const fonts = [
           {
             name: "sans serif",
-            data: fontData,
             weight: 700,
             style: "normal"
           }
diff --git a/dist/index.node.js b/dist/index.node.js
index fb6eba8c65ad3101aac24497d87b6514e85b7c7d..655ffcdbbf174decd340ce76dff722cdafb9d399 100644
--- a/dist/index.node.js
+++ b/dist/index.node.js
@@ -18676,6 +18676,8 @@ var Resvg2 = class extends Resvg {
 import { Readable } from "stream";
 import fs from "fs";
 import { fileURLToPath } from "url";
+import { dirname as __assetDirname, join as __assetJoin } from "path";
+const __assetDir = __assetDirname(fileURLToPath(import.meta.url));
 
 // src/emoji/index.ts
 var U200D = String.fromCharCode(8205);
@@ -18804,14 +18806,23 @@ async function render(satori2, resvg, opts, defaultFonts, element) {
       value: options.width
     }
   });
-  return resvgJS.render().asPng();
+  try {
+    const renderedImage = resvgJS.render();
+    try {
+      return renderedImage.asPng();
+    } finally {
+      renderedImage.free();
+    }
+  } finally {
+    resvgJS.free();
+  }
 }
 
 // src/index.node.ts
 var satori = Cl.default || Cl;
-var fontData = fs.readFileSync(fileURLToPath(`${import.meta.url}/../noto-sans-v27-latin-regular.ttf`));
-var yoga_wasm = fs.readFileSync(fileURLToPath(`${import.meta.url}/../yoga.wasm`));
-var resvg_wasm = fs.readFileSync(fileURLToPath(`${import.meta.url}/../resvg.wasm`));
+var fontData = fs.readFileSync(__assetJoin(__assetDir, "noto-sans-v27-latin-regular.ttf"));
+var yoga_wasm = fs.readFileSync(__assetJoin(__assetDir, "yoga.wasm"));
+var resvg_wasm = fs.readFileSync(__assetJoin(__assetDir, "resvg.wasm"));
 var initializedResvg = initWasm(resvg_wasm);
 var initializedYoga = initYoga(yoga_wasm).then((yoga2) => Ll(yoga2));
 var ImageResponse = class {
diff --git a/dist/types.d.ts b/dist/types.d.ts
index dde26cc6bbee29507d79bd0a9ec6c27719420038..d075e99d4ac094f35379c8a9014e6ca0b3c8be0f 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -28,9 +28,8 @@ declare type ImageOptions = {
      * A list of fonts to use.
      *
      * @type {{ data: ArrayBuffer; name: string; weight?: 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900; style?: 'normal' | 'italic' }[]}
-     * @default Noto Sans Latin Regular.
      */
-    fonts?: SatoriOptions['fonts'];
+    fonts: SatoriOptions['fonts'];
     /**
      * Using a specific Emoji style. Defaults to `twemoji`.
      *
