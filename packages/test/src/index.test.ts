import { describe, expect, it } from 'vitest'

// TODO: Remove this test and add real tests for your package
// It's just a placeholder so vitest doesn't fail due to no tests existing
describe('@universe/test', () => {
  it('runs tests', () => {
    expect(true).toBe(true)
  })
})
