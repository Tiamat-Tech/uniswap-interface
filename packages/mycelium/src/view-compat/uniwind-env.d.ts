/// <reference types="uniwind/types" />

// Pulls in uniwind's React Native type augmentations (adds `className` to View,
// Text and friends) for ViewCompat.native.tsx — same pattern as
// src/shimmer/uniwind-env.d.ts and apps/mobile/uniwind-env.d.ts.
