/// <reference types="uniwind/types" />

// Pulls in uniwind's React Native type augmentations (adds `className` to View,
// Text, Pressable, etc.) so the .native segmented-control files typecheck.
// Mirrors apps/mobile/uniwind-env.d.ts.
