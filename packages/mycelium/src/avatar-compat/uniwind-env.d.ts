/// <reference types="uniwind/types" />

// Pulls in uniwind's React Native type augmentations (adds `className` to View
// and friends) for the `.native.tsx` leg — same pattern as
// checkbox-compat/uniwind-env.d.ts and segmented-control-compat/uniwind-env.d.ts.
