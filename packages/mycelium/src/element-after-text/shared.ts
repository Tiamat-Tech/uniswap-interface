import type { TextCompatProps } from '../text-compat/props'

/** Legacy `DEFAULT_TEXT_PROPS` — caller `textProps` spread over these. */
export const DEFAULT_TEXT_PROPS: TextCompatProps = {
  color: '$neutral1',
  variant: 'body2',
}
