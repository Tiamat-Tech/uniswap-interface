/// <reference types="uniwind/types" />

// Pulls in uniwind's React Native type augmentations (adds `className` to View
// and friends) for the `.native.tsx` legs — same pattern as
// segmented-control-compat/uniwind-env.d.ts and apps/mobile/uniwind-env.d.ts.
