/**
 * Rebuilt on mycelium (INFRA-3340): ResizeObserver + measured pixel height +
 * CSS transition on web, functional stub on native. Re-exported here so the
 * 25 existing `ui/src` consumers (barrel and deep imports) are untouched.
 */
export { HeightAnimator, type HeightAnimatorProps } from '@universe/mycelium/height-animator'
