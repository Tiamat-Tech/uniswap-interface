// Accepted delta on a public export: this renders the compat's own inline SVG, not ui's
// `SpinningLoader`. Same size and colour, different DOM.
export { ThemedSpinnerCompat as ThemedSpinningLoader } from '@universe/mycelium/button-frame-compat'
export type { ThemedSpinnerCompatProps as ThemedSpinningLoaderProps } from '@universe/mycelium/button-frame-compat'
