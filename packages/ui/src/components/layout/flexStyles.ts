// Re-exported (not redefined) so `ui/src`'s `flexStyles` stays a single source of truth with
// mycelium's drop-in (INFRA-3753) instead of a duplicate value-identical object.
export { flexStyles } from '@universe/mycelium/flex-compat'
